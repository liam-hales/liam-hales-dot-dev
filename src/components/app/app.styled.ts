import styled from 'styled-components';
import { Footer } from '..';

/**
 * The styled `Footer` component used
 * for the `App` component
 */
export const StyledFooter = styled(Footer)`
  padding-top: 100px;
`;
