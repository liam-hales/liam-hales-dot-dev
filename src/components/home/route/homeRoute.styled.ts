import { styled } from '@mui/material';
import { AboutMe, SkillAreas, ProStatement, StillInterested } from '../..';

/**
 * The styled `AboutMe` component used
 * for the `HomeRoute` component
 */
export const StyledAboutMe = styled(AboutMe)`
  padding-top: 60px;
`;

/**
 * The styled `SkillAreas` component used
 * for the `HomeRoute` component
 */
export const StyledSkillAreas = styled(SkillAreas)`
  width: 100%;
  padding-top: 68px;
`;

/**
 * The styled `ProStatement` component used
 * for the `HomeRoute` component
 */
export const StyledProStatement = styled(ProStatement)`
  padding-top: 96px;
`;

/**
 * The styled `StillInterested` component used for
 * the `HomeRoute` component View Currciclum Viate
 */
export const StyledStillInterested = styled(StillInterested)`
  padding-top: 96px;
`;
