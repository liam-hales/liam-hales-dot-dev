import { styled, Backdrop } from '@mui/material';

/**
 * The styled `Backdrop` component used for
 * the `Loader` component backdrop
 */
export const StyledBackgrop = styled(Backdrop)`
  z-index: 1;
`;
