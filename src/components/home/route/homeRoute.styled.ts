import styled from 'styled-components';
import { AboutMe } from '../..';

/**
 * The styled `AboutMe` component used
 * for the `HomeRoute` component
 */
export const StyledAboutMe = styled(AboutMe)`
  padding-top: 60px;
`;
