import styled from 'styled-components';
import { Text } from '../../common';

/**
 * The styled `Text` component used
 * for the `ViewCurrciclumViate` component
 */
export const StyledText = styled(Text)`
  max-width: 340px;
  padding-top: 8px;
  padding-bottom: 32px;
  text-align: center;
`;
