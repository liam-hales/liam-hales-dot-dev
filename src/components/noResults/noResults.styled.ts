import { styled } from '@mui/material';
import { Text } from '../common';

/**
 * The styled `Text` component used for
 * the `NoResults` component text
 */
export const StyledText = styled(Text)`
  max-width: 300px;
  padding-top: 16px;
  text-align: center;
`;
