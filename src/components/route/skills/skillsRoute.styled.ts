import { styled } from '@mui/material';
import { Skills } from '../..';

/**
 * The styled `Skills` component used
 * for the `SkillsRooute` component
 */
export const StyledSkills = styled(Skills)`
  padding-top: 50px;
`;
